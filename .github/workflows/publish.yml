name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@efcec4cdf88255d5fb8f17c475b6e42ff957076a # main
    secrets: inherit
